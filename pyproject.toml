[project]
name = "data-monitor"
version = "0.1.0"
description = "반도체 시료 생산주문관리 시스템 - 데이터 모니터링 Tool (읽기 전용 콘솔 관리자 도구)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
